# Copy to keystore.properties and fill in real values. Never commit keystore.properties or *.jks.
storeFile=keystore/cam-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=cam_release
keyPassword=YOUR_KEY_PASSWORD
